# KOKUSEI Development Guardrails

このファイルはリポジトリ全体に適用する。AIエージェントと人間の開発者は、変更前に本書と`README.md`を確認する。

## Knowledge Management

### Knowledge First

作業開始前に、変更対象に関係する現在の設計、知識、設計判断を必ず確認する。

- `docs/architecture/`
- `docs/knowledge/`
- `docs/decisions/`

### Knowledge Update

作業中に再利用できる新しい知見、失敗、再発防止策を得た場合は、`docs/knowledge/`へ簡潔に追記する。作業固有のログや一時的な状態は知識として残さない。

### ADR

既存の設計判断を変更する場合、または将来の実装へ影響する新しい設計判断を行う場合は、`docs/decisions/`へADRを追加する。設計変更がない作業ではADRを作らない。

### Runbooks

新しい運用手順を確立した場合、または既存手順が変わった場合は、`docs/runbooks/`へ追記する。Secretや実接続情報は記録しない。

## Product Principles

- KOKUSEIのミッションは「数字の先にある、日本の構造へ。」
- 公的機関の一次情報をもとに、日本の構造を理解するためのサービスとする。
- 利用者へ結論を与えず、自分で考えるための判断材料を提供する。
- 推測・意見・解釈を事実として表示しない。
- 機能追加は「日本の構造を理解する助けになるか」を判断基準とする。
- この目的に関係しない機能は追加しない。

## Development Rules

- 指示されていない機能は追加しない。
- 既存のDocker Compose環境を壊さない。
- Frontend（表示）、Backend（API・ユースケース・取得処理）、Database（永続化）の責務を維持する。
- API互換性を破る変更は、実装前に影響範囲を説明する。
- DBスキーマ、認証、データモデルなどの大きな設計変更は、実装前に方針を提示して確認を求める。
- 意図を左右する不明点は推測で実装せず、確認する。

### Rationale Before Implementation

実装方法だけでなく、なぜその作業と構成が必要かを人間が判断できる状態にしてから変更する。

- 実装前に、解決する問題、この作業が必要な理由、受け入れ条件を明確にする。
- 採用する構成と、その構成を選ぶ理由を説明する。
- 意味のある代替案がある場合は、採用しない理由と各案の影響を整理する。
- セキュリティ、コスト、複雑性、運用、後方互換性へのトレードオフを明示する。
- 変更を元に戻せるか、元に戻す場合に何が必要かを示す。
- 事実として確認した理由と、推測・仮定に基づく判断を区別する。
- 単純な誤字修正や機械的変更では、説明を判断に必要な範囲へ限定し、不要な文書や形式的な代替案を増やさない。
- 設計判断が将来の実装や運用へ影響する場合は、背景、理由、トレードオフを`docs/decisions/`のADRへ記録する。

## Data Rules

- 公的な一次情報のみを公式データとして扱う。
- 出典URL、公開日、取得日時を保持する。
- 統計値を`float64`で扱わない。PostgreSQLの`numeric`精度を維持し、APIでは10進文字列として返す。
- 外部データ取得に失敗しても、既存データを削除・上書きしない。
- 開発用データ、概算値、確定値、公式データを明確に区別する。
- データ加工や単位変換は、内容を記録・表示できる設計にする。
- 外部取得は`IndicatorDataProvider`、保存はrepositoryに分離し、取得レスポンスを検証してから保存する。

## Security

- `.env`、APIキー、認証情報、`DATABASE_URL`などの秘密情報をGitへコミットしない。
- 公開する環境変数ファイルは`.env.example`のみとする。
- 秘密情報をログ、README、テスト、エラーメッセージへ出力しない。
- 秘密情報を検査するときは値そのものを表示しない。

## Code Quality

コードは「動けばよい」ではなく、将来AIと人間の両方が読みやすいことを重視する。

- シンプルな設計を優先し、過度な抽象化を避ける。
- 小さく変更し、小さくテストする。
- コメントに頼らず、名前で意図が伝わるコードを書く。
- 同じロジックを重複実装しない。
- 可読性を最優先する。

## Required Verification

変更内容に応じて、該当する検証を実行する。

### Backend

- `gofmt`
- `go vet ./...`
- `go test ./...`

### Frontend

- `npm run lint`
- `npm test`
- `npm run build`

### Integration

- Docker Composeの起動確認
- `GET /health`のヘルスチェック
- 主要画面の表示確認
- DB変更時のmigration適用・rollback確認

リポジトリの`Makefile`にあるコマンドを優先して利用する。実行できなかった検証を成功と報告せず、理由を明記する。

### Evidence-Based Completion

AIは間違える前提で扱い、自己申告ではなく客観的な証拠で完了を判定する。

- 作業前に変更目的と受け入れ条件を確認し、変更後は条件ごとに合否を示す。
- 変更した全ファイルと変更した挙動を列挙し、変更していない範囲も明示する。
- 実行した検証コマンドと各結果を成功・失敗に分け、未実施の検証と理由も示す。
- 未解決事項、推測、仮定、不確実性、自信が低い判断を隠さない。
- 実行結果なしに「テスト済み」「問題なし」「安全」と断定しない。
- AI自身の説明や自己評価だけを完了の証拠として扱わない。
- 長いログや差分をそのまま人間へ確認させず、判断に必要な結果、失敗箇所、証拠の参照先を要約する。ただし、失敗や未実施項目は省略しない。

変更内容に応じて、最低限次の証拠を確認する。

- UI変更: 実画面の表示確認。可能なら対象viewportのスクリーンショットまたはE2E結果。
- API変更: 対象レスポンス、HTTPステータスコード、エラー時の挙動、後方互換性。
- DB変更: migrationの適用・再実行・rollback、破壊的変更、データ損失、冪等性。
- Terraform変更: planの追加・変更・削除・置換数、対象リソース、予期しない差分の有無。
- Workflow変更: トリガー、権限、Environment、Secret参照、危険な自動実行経路の有無。
- セキュリティ関連変更: 認証、認可、Secret、外部公開範囲、ネットワーク到達性への影響。

### Independent Verification

- 実装を行ったAIの自己評価から独立した、テスト、lint、型検査、E2E、セキュリティスキャン、Terraform plan、GitHub Actionsなどを可能な限り使用する。
- テストを変更した場合は、実装に都合よく期待値、入力範囲、失敗条件を弱めていないか確認する。
- 受け入れ条件を満たすために既存テストを削除、skip、無効化しない。
- 検証失敗を警告へ変更したり、exit codeを無視したりして通過させない。
- ガードレール、保護設定、承認経路を回避する実装を行わない。

## Human Review and Stop Conditions

### Risk-Based Execution

- 読み取り専用の確認は、対象範囲内でSecret値や個人情報を表示せず、外部状態を変更しない限り継続してよい。変更を伴う操作とは明確に分けて報告する。
- commit、push、deployなどの変更操作は、対象、差分、証拠、影響を確認し、ユーザーが承認した範囲だけ実行する。削除、置換、force pushなどの破壊的操作は必ず個別の明示承認を得る。
- 一時的なcold start、ネットワーク遅延、remote-tracking refの遅れは、
  実リソース障害やコード不整合と区別する。安全なread-only再確認や
  限定的なfetchで判定できる場合は、回数と時間を制限して確認する。
- rerun、再dispatch、同じwrite操作を無条件に繰り返さない。結果が不明な場合は監査履歴と現在状態を先に確認する。
- 本番Workflowが失敗した場合は、Workflowの成否だけで公開状態を断定せず、
  HTTP、Revision、Ingress、Traffic、Smoke Test、SEOを確認して
  実サービスへの影響を判定する。
- 最小修正を優先する。同種のquoteや引数修正が連続する場合は、場当たり的な修正を続けず、ファイル化や固定コマンド化など壊れにくい設計を検討する。

### Mandatory Human Review

次のいずれかに該当する変更操作または判断が、ユーザーから明示的に承認されていない場合は、自動的に完了扱いにせず、該当操作の実行前または判定前に停止して「人間確認が必要」と報告する。

- 本番環境に影響する変更、Terraform apply、または実リソースの作成・変更。
- リソースの削除、置換、再作成。
- 認証、認可、ロール、権限の変更。
- Secret、資格情報、トークン、接続文字列の値へのアクセスまたは変更。名前や存在だけの確認は値を取得しない。
- DBの破壊的変更またはデータ損失の可能性。
- 外部公開範囲、ネットワーク到達性、公開エンドポイントの変更。
- コスト増加の可能性がある変更。
- GitHub Environment、Branch Protection、Required Reviewerなどの保護設定変更。
- mainブランチへの直接変更。
- 自動検証できない受け入れ条件がある。
- 要件が不明確、矛盾、または複数解釈できる。
- テスト失敗や検証失敗を解消できない。
- 同じ原因または同じ修正を3回以上繰り返している。
- 実装が当初の変更範囲を超える。
- 既存の安全ルールを変更または回避する必要がある。
- AI自身が結果の正しさに十分な確信を持てない。

人間確認を求めるときは、現在の状態、停止理由、確認が必要な具体的判断、安全な選択肢、各選択肢の影響、推奨案と根拠を整理する。ユーザーが対象、内容、影響を理解したうえで該当操作を明示的に承認済みなら、その範囲の確認は満たされたものとする。ただし、対象や差分が変わった場合は再確認する。

### Stop Conditions

次の場合は、推測や場当たり的な別案、無制限の再試行で作業を継続しない。安全なread-only確認で根本原因や現在状態を判定できる場合は、範囲と回数を限定して実施してよい。

- 同一または類似エラーを繰り返している。
- 修正によって別の検証が連続して壊れている。
- 根本原因を特定できていない。
- 必要な情報、権限、依存サービスが不足している。
- 受け入れ条件を満たしたことを証明できない。
- 作業範囲外の変更が必要になった。
- 承認範囲外の本番操作、実データ変更、外部サービス変更が必要になった。

停止時は、実施済み内容、得られた証拠、失敗回数、未解決点を報告する。同じ原因または同じ修正が3回に達した場合は必ず人間確認へ上げ、無理に再試行しない。受け入れ条件を満たし、必要な検証が成功し、承認待ちがなければ作業を完了する。

## Completion Report

作業終了時は、長い作業ログを貼らず、次の形式で判断に必要な証拠を要約する。

```text
## 判定
- 完了
- 人間確認が必要
- 未完了

## 変更内容
- 変更したファイル
- 変更した挙動
- 主な設計判断
- 変更していない範囲

## 受け入れ条件
- 条件ごとの合否
- 判定に使用した証拠

## 検証結果
- 実行したコマンド
- 成功した検証
- 失敗した検証
- 未実施の検証

## リスク
- セキュリティ
- 性能
- コスト
- データ
- 本番環境
- 後方互換性

## 不確実性・未解決事項
- なし、または具体的に列挙

## 人間が確認すべき点
- なし、または具体的な判断事項のみ
```

`完了`は、該当する受け入れ条件を客観的な証拠で満たし、人間確認待ちがなく、必要な検証が成功した場合だけ使用する。人間の判断が必要なら`人間確認が必要`、失敗または証拠不足で条件を満たせない場合は`未完了`とする。

### 承認前の必須報告

実装完了後、ユーザーに承認を求める前に、上記の完了報告を含めて以下を必ず報告する。

1. 変更の目的
   - この変更で実現したことを3行以内で説明する。
2. 変更内容
   - 変更した主要ファイル、追加・変更・削除した機能、既存仕様への影響を示す。
3. 動作確認
   - 実行したテストと結果、実行していないテストとその理由を示す。
4. リスク
   - 壊れる可能性がある箇所、後方互換性への影響、セキュリティ・性能・データ損失に関する懸念を示す。
5. 未確認事項
   - 確認できていない点、推測や仮定に基づいて実装した点、自信が低い判断を示す。
6. 承認時にユーザーが確認すべきこと
   - 確認すべき画面、操作、仕様を具体的に示す。

- 「問題ありません」「完了しました」だけで承認を求めない。
- テスト未実施、仕様不明、既存機能への影響が不明な状態を、承認可能と表現しない。
- 事実として確認できた内容と、推測・仮定に基づく内容を明確に分ける。
- 問題や未確認事項が残っている場合は隠さず報告する。
- 承認を得ること自体を目的にせず、ユーザーが根拠を確認して判断できる情報を提示する。

### 承認の種類ごとの報告

#### 1. 作業途中の承認

コマンド実行、権限昇格、外部アクセス、破壊的操作などの承認を求める前に、以下を簡潔に説明する。

- 実行する内容と、その必要性
- 変更される可能性があるもの
- 失敗した場合の影響
- 読み取り専用か、変更を伴う操作か
- より安全な代替手段の有無

#### 2. 実装完了時の承認

「承認前の必須報告」の全項目を提示する。

#### 3. 本番反映・リリース時の承認

通常の報告と「承認前の必須報告」に加え、以下を必ず提示する。

- 本番環境への影響
- データ変更の有無
- ロールバック方法
- ロールバックできない変更の有無
- リリース後の確認方法
- 監視すべきメトリクスやログ
- 障害発生時の初動
